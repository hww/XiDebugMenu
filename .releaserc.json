{
  "tagFormat": "v${version}",
  "plugins": [
    ["@semantic-release/commit-analyzer", { "preset": "angular" }],
    "@semantic-release/release-notes-generator",
    ["@semantic-release/changelog", { "preset": "angular" }],
    ["@semantic-release/npm", { "npmPublish": false, "pkgRoot": "Assets/varp_subsystem" }],
    ["@semantic-release/git", {
      "assets": ["Assets/varp_subsystem/package.json", "CHANGELOG.md"],
      "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
    }],
    ["@semantic-release/github", {
      "assets": [
        {"path": "varp_subsystem.unitypackage", "label": "varp_subsystem v${nextRelease.version}"}
      ]
    }]
  ]
}
